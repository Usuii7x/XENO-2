--[[
    KEYLESS RAYFIELD SCRIPT - COMPLETO
    Para uso no Roblox Examination
]]

-- Verificar se o Rayfield está carregado
if not game:GetService("CoreGui"):FindFirstChild("Rayfield") then
    -- Carregar Rayfield automaticamente
    local RayfieldLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/shlexware/Rayfield/main/source"))()
    window = RayfieldLib:CreateWindow({
        Name = "Examination Script",
        LoadingTitle = "Carregando Script...",
        LoadingSubtitle = "By Script Hub",
        ConfigurationSaving = {
            Enabled = true,
            FolderName = "ExaminationScript",
            FileName = "Settings"
        },
        Discord = {
            Enabled = false,
            Invite = "noinvite",
            RememberJoins = true
        },
        KeySystem = false
    })
else
    window = RayfieldLib:CreateWindow({
        Name = "Examination Script",
        LoadingTitle = "Carregando Script...",
        LoadingSubtitle = "By Script Hub",
        ConfigurationSaving = {
            Enabled = true,
            FolderName = "ExaminationScript",
            FileName = "Settings"
        },
        KeySystem = false
    })
end

-- Criar Tabs
local MainTab = window:CreateTab("Principal", 4483362458)
local CombatTab = window:CreateTab("Combate", 4483362458)
local MovementTab = window:CreateTab("Movimento", 4483362458)
local VisualTab = window:CreateTab("Visuais", 4483362458)
local MiscTab = window:CreateTab("Extras", 4483362458)

-- Variáveis Globais
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualInputManager = game:GetService("VirtualInputManager")

-- Status das funções
local FullBrightEnabled = false
local KillAuraEnabled = false
local AutoCompleteTaskEnabled = false
local InfiniteBreathEnabled = false
local InfiniteAmmoEnabled = false
local SpeedEnabled = false
local JumpPowerEnabled = false
local ESPEnabled = false
local AutoCollectEnabled = false
local NoClipEnabled = false
local FlyEnabled = false
local TeleportEnabled = false

-- Configurações
local KillAuraRange = 15
local WalkSpeedValue = 50
local JumpPowerValue = 100
local FlySpeed = 50
local TeleportTarget = nil

-- Função FullBright
local function SetFullBright(Enabled)
    if Enabled then
        game:GetService("Lighting").Brightness = 2
        game:GetService("Lighting").ClockTime = 12
        game:GetService("Lighting").FogEnd = 100000
        game:GetService("Lighting").GlobalShadows = false
        game:GetService("Lighting").OutdoorAmbient = Color3.fromRGB(128, 128, 128)
    else
        game:GetService("Lighting").Brightness = 0.5
        game:GetService("Lighting").ClockTime = 0
        game:GetService("Lighting").FogEnd = 100
        game:GetService("Lighting").GlobalShadows = true
        game:GetService("Lighting").OutdoorAmbient = Color3.fromRGB(0, 0, 0)
    end
end

-- Função KillAura
local function GetNearestEnemy()
    local Nearest = nil
    local ShortestDistance = KillAuraRange
    
    for _, Player in pairs(Players:GetPlayers()) do
        if Player ~= LocalPlayer and Player.Character and Player.Character:FindFirstChild("Humanoid") and Player.Character.Humanoid.Health > 0 then
            local Distance = (LocalPlayer.Character.HumanoidRootPart.Position - Player.Character.HumanoidRootPart.Position).Magnitude
            if Distance < ShortestDistance then
                ShortestDistance = Distance
                Nearest = Player
            end
        end
    end
    
    return Nearest
end

local function KillAuraAttack()
    if not KillAuraEnabled then return end
    
    local Target = GetNearestEnemy()
    if Target and Target.Character then
        local Humanoid = Target.Character:FindFirstChild("Humanoid")
        if Humanoid and Humanoid.Health > 0 then
            -- Simular ataque (ajustar conforme o jogo)
            game:GetService("ReplicatedStorage"):FindFirstChild("AttackEvent"):FireServer(Target.Character)
            -- Ou usar VirtualInputManager
            VirtualInputManager:SendMouseButtonEvent(Vector2.new(500, 300), 0, true, Enum.UserInputType.MouseButton1, 0)
            task.wait(0.1)
            VirtualInputManager:SendMouseButtonEvent(Vector2.new(500, 300), 0, false, Enum.UserInputType.MouseButton1, 0)
        end
    end
end

-- Função Auto Complete Task
local function CompleteTasks()
    if not AutoCompleteTaskEnabled then return end
    
    -- Procurar por tasks no jogo (ajustar conforme necessário)
    for _, Object in pairs(workspace:GetDescendants()) do
        if Object.Name:find("Task") or Object.Name:find("Mission") or Object.Name:find("Quest") then
            if Object:FindFirstChild("ProximityPrompt") then
                fireproximityprompt(Object.ProximityPrompt)
            elseif Object:IsA("ClickDetector") then
                Object:Click()
            end
        end
    end
end

-- Função Infinite Breath
local function SetInfiniteBreath(Enabled)
    if Enabled then
        local Humanoid = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid")
        if Humanoid then
            Humanoid:SetAttribute("Breath", 100)
        end
    end
end

-- Função Infinite Ammo
local function SetInfiniteAmmo(Enabled)
    if Enabled then
        -- Procurar pelo inventário/arma do jogador
        local Backpack = LocalPlayer.Backpack
        local Character = LocalPlayer.Character
        
        for _, Item in pairs(Backpack:GetChildren()) do
            if Item:IsA("Tool") and Item:FindFirstChild("Ammo") then
                Item.Ammo.Value = math.huge
            end
        end
        
        for _, Item in pairs(Character:GetChildren()) do
            if Item:IsA("Tool") and Item:FindFirstChild("Ammo") then
                Item.Ammo.Value = math.huge
            end
        end
    end
end

-- Função ESP
local function CreateESP(Player)
    if not Player or Player == LocalPlayer then return end
    
    local function CreateBillboard(Char)
        if not Char or Char:FindFirstChild("Head") then return end
        
        local Billboard = Instance.new("BillboardGui")
        Billboard.Name = "ESP_" .. Player.Name
        Billboard.Adornee = Char.Head
        Billboard.Size = UDim2.new(0, 200, 0, 50)
        Billboard.StudsOffset = Vector3.new(0, 2, 0)
        Billboard.AlwaysOnTop = true
        
        local TextLabel = Instance.new("TextLabel")
        TextLabel.Size = UDim2.new(1, 0, 1, 0)
        TextLabel.BackgroundTransparency = 1
        TextLabel.Text = Player.Name .. " (" .. math.floor((Char.Head.Position - LocalPlayer.Character.Head.Position).Magnitude) .. "m)"
        TextLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
        TextLabel.TextScaled = true
        TextLabel.Parent = Billboard
        
        Billboard.Parent = Char.Head
        return Billboard
    end
    
    Player.CharacterAdded:Connect(function(Char)
        task.wait(0.5)
        CreateBillboard(Char)
    end)
    
    if Player.Character then
        CreateBillboard(Player.Character)
    end
end

-- Função Auto Collect
local function AutoCollect()
    if not AutoCollectEnabled then return end
    
    for _, Item in pairs(workspace:GetDescendants()) do
        if Item:IsA("Model") and (Item.Name:find("Coin") or Item.Name:find("Item") or Item.Name:find("Collect")) then
            if Item:FindFirstChild("TouchInterest") then
                LocalPlayer.Character.HumanoidRootPart.CFrame = Item.PrimaryPart.CFrame
                task.wait(0.1)
            end
        end
    end
end

-- Função NoClip
local function SetNoClip(Enabled)
    if Enabled then
        LocalPlayer.Character.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Climbing, false)
        LocalPlayer.Character.Humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
        LocalPlayer.Character.Humanoid:SetStateEnabled(Enum.HumanoidStateType.GettingUp, false)
        LocalPlayer.Character.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, false)
        
        RunService.Stepped:Connect(function()
            if NoClipEnabled and LocalPlayer.Character then
                for _, Part in pairs(LocalPlayer.Character:GetDescendants()) do
                    if Part:IsA("BasePart") then
                        Part.CanCollide = false
                    end
                end
            end
        end)
    else
        for _, Part in pairs(LocalPlayer.Character:GetDescendants()) do
            if Part:IsA("BasePart") then
                Part.CanCollide = true
            end
        end
    end
end

-- Função Fly
local FlyConnection
local FlyBodyVelocity

local function SetFly(Enabled)
    if Enabled then
        FlyBodyVelocity = Instance.new("BodyVelocity")
        FlyBodyVelocity.MaxForce = Vector3.new(10000, 10000, 10000)
        FlyBodyVelocity.P = 10000
        FlyBodyVelocity.Parent = LocalPlayer.Character.HumanoidRootPart
        
        FlyConnection = RunService.RenderStepped:Connect(function()
            local Camera = workspace.CurrentCamera
            local MoveDirection = Vector3.new()
            
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then
                MoveDirection = MoveDirection + Camera.CFrame.LookVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then
                MoveDirection = MoveDirection - Camera.CFrame.LookVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then
                MoveDirection = MoveDirection + Camera.CFrame.RightVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then
                MoveDirection = MoveDirection - Camera.CFrame.RightVector
            end
            
            FlyBodyVelocity.Velocity = MoveDirection * FlySpeed
        end)
    else
        if FlyConnection then FlyConnection:Disconnect() end
        if FlyBodyVelocity then FlyBodyVelocity:Destroy() end
    end
end

-- Função Teleport
local function TeleportToPlayer(Player)
    if Player and Player.Character then
        LocalPlayer.Character.HumanoidRootPart.CFrame = Player.Character.HumanoidRootPart.CFrame
    end
end

-- Criar UI Elements
MainTab:CreateButton({
    Name = "Reconectar",
    Callback = function()
        game:GetService("TeleportService"):Teleport(game.PlaceId)
    end
})

MainTab:CreateSlider({
    Name = "WalkSpeed",
    Range = {16, 250},
    Increment = 1,
    Suffix = "Speed",
    CurrentValue = 16,
    Flag = "WalkSpeed",
    Callback = function(Value)
        WalkSpeedValue = Value
        if SpeedEnabled then
            LocalPlayer.Character.Humanoid.WalkSpeed = Value
        end
    end
})

MainTab:CreateSlider({
    Name = "JumpPower",
    Range = {50, 500},
    Increment = 5,
    Suffix = "Power",
    CurrentValue = 50,
    Flag = "JumpPower",
    Callback = function(Value)
        JumpPowerValue = Value
        if JumpPowerEnabled then
            LocalPlayer.Character.Humanoid.JumpPower = Value
        end
    end
})

-- Combat Tab
CombatTab:CreateToggle({
    Name = "Kill Aura",
    CurrentValue = false,
    Flag = "KillAura",
    Callback = function(Value)
        KillAuraEnabled = Value
        while KillAuraEnabled do
            KillAuraAttack()
            task.wait(0.1)
        end
    end
})

CombatTab:CreateSlider({
    Name = "Kill Aura Range",
    Range = {5, 50},
    Increment = 1,
    Suffix = "Studs",
    CurrentValue = 15,
    Flag = "KillAuraRange",
    Callback = function(Value)
        KillAuraRange = Value
    end
})

-- Movement Tab
MovementTab:CreateToggle({
    Name = "Speed Hack",
    CurrentValue = false,
    Flag = "SpeedHack",
    Callback = function(Value)
        SpeedEnabled = Value
        if Value then
            LocalPlayer.Character.Humanoid.WalkSpeed = WalkSpeedValue
        else
            LocalPlayer.Character.Humanoid.WalkSpeed = 16
        end
    end
})

MovementTab:CreateToggle({
    Name = "Jump Power Hack",
    CurrentValue = false,
    Flag = "JumpPowerHack",
    Callback = function(Value)
        JumpPowerEnabled = Value
        if Value then
            LocalPlayer.Character.Humanoid.JumpPower = JumpPowerValue
        else
            LocalPlayer.Character.Humanoid.JumpPower = 50
        end
    end
})

MovementTab:CreateToggle({
    Name = "No Clip",
    CurrentValue = false,
    Flag = "NoClip",
    Callback = function(Value)
        NoClipEnabled = Value
        SetNoClip(Value)
    end
})

MovementTab:CreateToggle({
    Name = "Fly",
    CurrentValue = false,
    Flag = "Fly",
    Callback = function(Value)
        FlyEnabled = Value
        SetFly(Value)
    end
})

MovementTab:CreateSlider({
    Name = "Fly Speed",
    Range = {10, 200},
    Increment = 5,
    Suffix = "Speed",
    CurrentValue = 50,
    Flag = "FlySpeed",
    Callback = function(Value)
        FlySpeed = Value
    end
})

-- Visual Tab
VisualTab:CreateToggle({
    Name = "FullBright",
    CurrentValue = false,
    Flag = "FullBright",
    Callback = function(Value)
        FullBrightEnabled = Value
        SetFullBright(Value)
    end
})

VisualTab:CreateToggle({
    Name = "ESP Players",
    CurrentValue = false,
    Flag = "ESP",
    Callback = function(Value)
        ESPEnabled = Value
        if Value then
            for _, Player in pairs(Players:GetPlayers()) do
                CreateESP(Player)
            end
            
            Players.PlayerAdded:Connect(function(Player)
                if ESPEnabled then
                    CreateESP(Player)
                end
            end)
        end
    end
})

-- Misc Tab
MiscTab:CreateToggle({
    Name = "Auto Complete Tasks",
    CurrentValue = false,
    Flag = "AutoComplete",
    Callback = function(Value)
        AutoCompleteTaskEnabled = Value
        while AutoCompleteTaskEnabled do
            CompleteTasks()
            task.wait(1)
        end
    end
})

MiscTab:CreateToggle({
    Name = "Infinite Breath",
    CurrentValue = false,
    Flag = "InfiniteBreath",
    Callback = function(Value)
        InfiniteBreathEnabled = Value
        while InfiniteBreathEnabled do
            SetInfiniteBreath(Value)
            task.wait(0.5)
        end
    end
})

MiscTab:CreateToggle({
    Name = "Infinite Ammo",
    CurrentValue = false,
    Flag = "InfiniteAmmo",
    Callback = function(Value)
        InfiniteAmmoEnabled = Value
        while InfiniteAmmoEnabled do
            SetInfiniteAmmo(Value)
            task.wait(0.5)
        end
    end
})

MiscTab:CreateToggle({
    Name = "Auto Collect Items",
    CurrentValue = false,
    Flag = "AutoCollect",
    Callback = function(Value)
        AutoCollectEnabled = Value
        while AutoCollectEnabled do
            AutoCollect()
            task.wait(0.5)
        end
    end
})

-- Dropdown para Teleport
local PlayersList = {}
local function UpdatePlayerList()
    PlayersList = {}
    for _, Player in pairs(Players:GetPlayers()) do
        if Player ~= LocalPlayer then
            table.insert(PlayersList, Player.Name)
        end
    end
end

MiscTab:CreateDropdown({
    Name = "Teleport to Player",
    Options = {},
    CurrentOption = "",
    Flag = "TeleportDropdown",
    Callback = function(Option)
        if Option ~= "" then
            for _, Player in pairs(Players:GetPlayers()) do
                if Player.Name == Option then
                    TeleportToPlayer(Player)
                end
            end
        end
    end
})

-- Atualizar lista de players
Players.PlayerAdded:Connect(function()
    task.wait(1)
    UpdatePlayerList()
    local Dropdown = RayfieldLib:Get("TeleportDropdown")
    if Dropdown then
        Dropdown:SetOptions(PlayersList)
    end
end)

Players.PlayerRemoving:Connect(function()
    task.wait(1)
    UpdatePlayerList()
    local Dropdown = RayfieldLib:Get("TeleportDropdown")
    if Dropdown then
        Dropdown:SetOptions(PlayersList)
    end
end)

-- Inicializar
UpdatePlayerList()
local Dropdown = RayfieldLib:Get("TeleportDropdown")
if Dropdown then
    Dropdown:SetOptions(PlayersList)
end

print("Script carregado com sucesso!")
