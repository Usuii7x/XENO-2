local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "Sniper Arena - Alysson Hub",
    LoadingTitle = "Carregando script...",
    LoadingSubtitle = "by Grok",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "SniperArenaHub",
        FileName = "AlyssonConfig"
    },
    Discord = {
        Enabled = false,
    },
    KeySystem = false
})

local MainTab = Window:CreateTab("Principal", 4483362458)
local CombatTab = Window:CreateTab("Combate", 4483362458)
local GunTab = Window:CreateTab("Armas", 4483362458)
local MiscTab = Window:CreateTab("Outros", 4483362458)

-- Variáveis globais
local aimbotEnabled = false
local aimKey = Enum.UserInputType.MouseButton2  -- Direito por padrão
local aimPart = "Head"
local fov = 150
local smoothness = 8
local teamCheck = true
local wallCheck = false

local player = game.Players.LocalPlayer
local camera = workspace.CurrentCamera
local runService = game:GetService("RunService")
local userInputService = game:GetService("UserInputService")

local aimConnection = nil

-- Função para pegar o alvo mais próximo
local function getClosestTarget()
    local closest = nil
    local shortestDistance = fov
    
    for _, plr in ipairs(game.Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("Humanoid") and plr.Character.Humanoid.Health > 0 then
            if teamCheck and plr.Team == player.Team then continue end
            
            local root = plr.Character:FindFirstChild("HumanoidRootPart")
            local head = plr.Character:FindFirstChild(aimPart)
            
            if root and head then
                local screenPos, onScreen = camera:WorldToViewportPoint(head.Position)
                if onScreen then
                    local mousePos = userInputService:GetMouseLocation()
                    local distance = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                    
                    if distance < shortestDistance then
                        -- Wall check simples
                        if wallCheck then
                            local ray = Ray.new(camera.CFrame.Position, (head.Position - camera.CFrame.Position).Unit * 500)
                            local hit, _ = workspace:FindPartOnRayWithIgnoreList(ray, {player.Character, camera})
                            if hit and hit:IsDescendantOf(plr.Character) == false then
                                continue
                            end
                        end
                        shortestDistance = distance
                        closest = head
                    end
                end
            end
        end
    end
    return closest
end

-- Aimbot Loop
local function startAimbot()
    if aimConnection then return end
    
    aimConnection = runService.RenderStepped:Connect(function()
        if not aimbotEnabled then return end
        
        -- Verifica se está segurando a tecla (MouseButton2 ou tecla escolhida)
        local isAiming = false
        if typeof(aimKey) == "EnumItem" then
            if aimKey.EnumType == Enum.UserInputType then
                isAiming = userInputService:IsMouseButtonPressed(aimKey)
            else
                isAiming = userInputService:IsKeyDown(aimKey)
            end
        end
        
        if not isAiming then return end
        
        local target = getClosestTarget()
        if target then
            local targetPos = camera:WorldToScreenPoint(target.Position)
            local mousePos = userInputService:GetMouseLocation()
            
            local moveX = (targetPos.X - mousePos.X) / smoothness
            local moveY = (targetPos.Y - mousePos.Y) / smoothness
            
            mousemoverel(moveX, moveY)
        end
    end)
end

local function stopAimbot()
    if aimConnection then
        aimConnection:Disconnect()
        aimConnection = nil
    end
end

-- ==================== TOGGLES ====================

CombatTab:CreateToggle({
    Name = "Aimbot Ativado",
    CurrentValue = false,
    Flag = "AimbotToggle",
    Callback = function(Value)
        aimbotEnabled = Value
        if Value then
            startAimbot()
        else
            stopAimbot()
        end
    end,
})

CombatTab:CreateDropdown({
    Name = "Parte do Corpo",
    Options = {"Head", "HumanoidRootPart", "UpperTorso"},
    CurrentOption = {"Head"},
    MultipleOptions = false,
    Flag = "AimPart",
    Callback = function(CurrentOption)
        aimPart = CurrentOption[1]
    end,
})

CombatTab:CreateSlider({
    Name = "FOV",
    Range = {50, 500},
    Increment = 10,
    CurrentValue = 150,
    Flag = "FOVSlider",
    Callback = function(Value)
        fov = Value
    end,
})

CombatTab:CreateSlider({
    Name = "Smoothness",
    Range = {1, 20},
    Increment = 1,
    CurrentValue = 8,
    Flag = "SmoothnessSlider",
    Callback = function(Value)
        smoothness = Value
    end,
})

CombatTab:CreateToggle({
    Name = "Team Check",
    CurrentValue = true,
    Flag = "TeamCheck",
    Callback = function(Value)
        teamCheck = Value
    end,
})

CombatTab:CreateToggle({
    Name = "Wall Check",
    CurrentValue = false,
    Flag = "WallCheck",
    Callback = function(Value)
        wallCheck = Value
    end,
})

-- Keybind customizável
CombatTab:CreateKeybind({
    Name = "Tecla do Aimbot (segure)",
    CurrentKeybind = "MouseButton2",
    HoldToUse = true,
    Flag = "AimKeybind",
    Callback = function(Key)
        aimKey = Key
    end,
})

-- ==================== ARMAS ====================

GunTab:CreateToggle({
    Name = "Munição Infinita",
    CurrentValue = false,
    Flag = "InfAmmo",
    Callback = function(Value)
        if Value then
            spawn(function()
                while Value and task.wait(0.1) do
                    local character = player.Character
                    if character then
                        local tool = character:FindFirstChildOfClass("Tool")
                        if tool then
                            -- Tenta vários caminhos comuns de ammo em jogos FPS
                            for _, v in pairs(tool:GetDescendants()) do
                                if v:IsA("IntValue") or v:IsA("NumberValue") then
                                    if v.Name:lower():find("ammo") or v.Name:lower():find("clip") or v.Name == "Bullets" then
                                        v.Value = 999
                                    end
                                end
                            end
                            
                            -- Alguns jogos usam attributes
                            pcall(function()
                                tool:SetAttribute("Ammo", 999)
                                tool:SetAttribute("CurrentAmmo", 999)
                            end)
                        end
                    end
                end
            end)
        end
    end,
})

GunTab:CreateToggle({
    Name = "No Recoil",
    CurrentValue = false,
    Flag = "NoRecoil",
    Callback = function(Value)
        -- Exemplo simples (funciona em muitos jogos)
        if Value then
            spawn(function()
                while Value and task.wait() do
                    local camera = workspace.CurrentCamera
                    camera.CFrame = camera.CFrame * CFrame.Angles(0,0,0) -- Mantém estável
                end
            end)
        end
    end,
})

GunTab:CreateToggle({
    Name = "No Spread",
    CurrentValue = false,
    Flag = "NoSpread",
    Callback = function(Value)
        -- Placeholder - ajuste conforme o jogo
        Rayfield:Notify({
            Title = "No Spread",
            Content = "Ativado (ajuste manual pode ser necessário)",
            Duration = 3
        })
    end,
})

GunTab:CreateButton({
    Name = "Fast Fire Rate",
    Callback = function()
        local tool = player.Character and player.Character:FindFirstChildOfClass("Tool")
        if tool then
            for _, v in pairs(tool:GetDescendants()) do
                if v.Name == "FireRate" or v.Name == "Cooldown" then
                    v.Value = 0.05
                end
            end
        end
        Rayfield:Notify({Title = "Fast Fire", Content = "Aplicado na arma atual", Duration = 3})
    end,
})

-- ==================== OUTROS ====================

MiscTab:CreateToggle({
    Name = "Infinite Jump",
    CurrentValue = false,
    Flag = "InfJump",
    Callback = function(Value)
        if Value then
            game:GetService("UserInputService").JumpRequest:Connect(function()
                if Value then
                    player.Character:FindFirstChildOfClass("Humanoid"):ChangeState("Jumping")
                end
            end)
        end
    end,
})

MiscTab:CreateSlider({
    Name = "Velocidade",
    Range = {16, 100},
    Increment = 4,
    CurrentValue = 16,
    Flag = "SpeedSlider",
    Callback = function(Value)
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.WalkSpeed = Value
        end
    end,
})

MiscTab:CreateButton({
    Name = "ESP Simples (Nome)",
    Callback = function()
        for _, plr in pairs(game.Players:GetPlayers()) do
            if plr ~= player and plr.Character then
                local billboard = Instance.new("BillboardGui")
                billboard.Adornee = plr.Character.Head
                billboard.Size = UDim2.new(0, 200, 0, 50)
                billboard.StudsOffset = Vector3.new(0, 3, 0)
                
                local text = Instance.new("TextLabel")
                text.Text = plr.Name
                text.BackgroundTransparency = 1
                text.TextColor3 = Color3.new(1,0,0)
                text.Size = UDim2.new(1,0,1,0)
                text.Parent = billboard
                billboard.Parent = plr.Character.Head
            end
        end
    end,
})

Rayfield:Notify({
    Title = "Script Carregado",
    Content = "Aimbot: Segure a tecla configurada (padrão Direito)",
    Duration = 5
})
